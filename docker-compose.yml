services:
  pea-energy-app:
    build: .
    ports:
      - "3000:3000"
    env_file:             
      - /home/hogzilla/.env.real
    volumes:
      - /home/hogzilla/data:/app/data
    restart: always