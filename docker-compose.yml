services:
  pea-energy-app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - API_BASE_URL=https://greatways.net
      - MAP_TILE_URL=https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png
    restart: always