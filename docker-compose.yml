services:
  pea-energy-app:
    build: .
    ports:
      - "3000:3000"
    env_file:             # เพิ่มคำสั่งนี้เข้ามาแทน environment:
      - ../.env           # ใส่ Path ชี้ถอยหลังออกไปข้างนอก 1 ระดับ (โฟลเดอร์ hogzilla)
    restart: always